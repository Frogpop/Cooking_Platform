import React,{Component} from "react";

export default class Formulas extends Component{
    render() {
        return(
            <div>
                We formuling
            </div>
        );
    }
}